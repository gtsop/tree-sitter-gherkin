============
Feature File
============

Feature: Feature Title
  
  This is the description

  Background:
    Given g
    When w
    Then t

  Scenario: My Scenario
    Given some Given state
    And some Given state
    When I do a When
    And I do a When
    Then I see a Then
    And I see a Then

---

(source_file
  (feature
    (title)
    (description (description_line))
    (background
      (steps
        (given (step))
        (when (step))
        (then (step))
      )
    )
    (scenario
      (title)
      (steps
        (given (step)
          (and (step))
        )
        (when (step)
          (and (step))
        )
        (then (step)
          (and (step))
        )
      )
    )
  )
)
